# テキストを作成
+text_1:
  action>: Text
  text: ${httpMethod}
  private: false

# オブジェクトを作成
+create_object_1:
  action>: CreateObject
  object: ${headers}
  private: false

# オブジェクトを作成
+create_object_2:
  action>: CreateObject
  object: ${rawBody}
  private: false

# テキストを作成
+text_2:
  action>: Text
  text: ${key1}
  private: false

# テキストを作成
+text_3:
  action>: Text
  text: ${key2}
  private: false